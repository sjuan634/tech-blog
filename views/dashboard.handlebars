<div>
  <div>
    {{#each user.posts as |post|}}
    <div>
      <h2 id="{{post.id}}">{{post.title}}</h2>
      <span>{{format_date post.createdAt}}</span>
    </div>
    {{/each}}
  </div>
  <a href="/new-post">
    <button type="button" id="new-post-btn">Add</button>
  </a>
</div>

<script src="../js/logout.js"></script>